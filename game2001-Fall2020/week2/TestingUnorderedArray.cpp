#include "UnorderedArray.h"
#include <iostream>
using namespace std;

/*
int main()
{
	UnorderedArray<int> testArray(3);





	cout << "growSize: " << testArray.GetGrowSize() << endl
		<< "numnElements: " << testArray.GetSize() << endl
		<< "maxSize: " << testArray.GetMaxSize() << endl;


	testArray.push(10);
	testArray.push(20);
	testArray.push(30);

	for (int i = 0; i < testArray.GetSize(); i++)
	{
		cout << testArray[i] << " ";
	}
	cout << endl;

	cout << "growSize: " << testArray.GetGrowSize() << endl
		<< "numnElements: " << testArray.GetSize() << endl
		<< "maxSize: " << testArray.GetMaxSize() << endl;

	testArray.SetGrowSize(5);

	testArray.push(40);
	for (int i = 0; i < testArray.GetSize(); i++)
	{
		cout << testArray[i] << " ";
	}
	cout << endl;
	cout << "growSize: " << testArray.GetGrowSize() << endl
		<< "numnElements: " << testArray.GetSize() << endl
		<< "maxSize: " << testArray.GetMaxSize() << endl;

	testArray.pop();
	for (int i = 0; i < testArray.GetSize(); i++)
	{
		cout << testArray[i] << " ";
	}
	cout << endl;
	cout << "growSize: " << testArray.GetGrowSize() << endl
		<< "numnElements: " << testArray.GetSize() << endl
		<< "maxSize: " << testArray.GetMaxSize() << endl;

	return 0;
}
*/